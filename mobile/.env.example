# URL of the backend API. Inlined into the mobile JS bundle at build time.
# DO NOT put secrets here — anything EXPO_PUBLIC_* is visible to anyone with the app.
EXPO_PUBLIC_API_URL=http://localhost:3001
