# Anthropic API key — required for Claude vision + recipe generation.
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Port the Express server listens on locally.
PORT=3001
